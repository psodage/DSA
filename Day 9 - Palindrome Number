//Day 9 - Palindrome Number
class Solution {
    public boolean isPalindrome(int x) {
        if (x<0) return false;
        int num=x;
        int res=0;
        while(x!=0){
            int digit=x%10;
            x=x/10;
            res=res*10+digit;
        }
        return res==num;
    }
}
